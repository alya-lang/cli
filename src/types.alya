# Types and data structures for cli
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct CliConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return CliConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end
